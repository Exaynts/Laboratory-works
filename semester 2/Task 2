#include <iostream>
#include <fstream>
using namespace std;

int main() {
    setlocale(LC_ALL, "ru");
    string inpath = "a.txt";
    string outpath = "a2.txt";
    ifstream inFile("a.txt");
    ofstream outFile("a2.txt");
    inFile.open(inpath);
    outFile.open(outpath);
    if (!inFile.is_open() or !outFile.is_open())
        cout << "Ошибка открытия файлов";
    else {
        double number;
        while (inFile >> number) {
            if (number != 0) {
            outFile << number << endl;
            }
        }
        inFile.close();
        outFile.close();
    }
    return 0;
}
